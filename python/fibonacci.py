#accept non negative integer (k)
#if none/too many/negative k then return value of 1 (error)
#accept plaintext string from user
#return ciphertext for user and return 0 
#nb. k=1 == k=27

alf = string.ascii_lowercase
msg = list(raw_input("Enter your message: "))
number = alf[]
encrypt = []

while True:	
	k = int(raw_input("Enter a non-negative integer: ")) 
	if k > 0:
		break

for char in msg:
	if char in alf:
		return number
		number + k = newnum 
		if newnum % 26 in number:
			return number
			encrypt.append(number)
	else:
		encrypt.append(char)

print encrypt

