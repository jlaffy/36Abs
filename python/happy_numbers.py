#Happy Numbers Project
def sumsqdigits(n):
	digits = list(str(n))
  	sqdList = []
	for int(x) in digits:
		sqd_digits = x*x
		sqdList.append(sqd)
	sumOfSquares = sum(sqdList)
	return sumOfSquares

def happy_checker(n):
	while sumOfSquares != 1: 
		sumOfSquares == str(n)

		if sumsqdigits() == 1:
		print "Yay! Happy " + str(n)

while sumList != 1:
		sumList = n
		sumsqdigits(sumList)

if sumlist == 1:
	print "Yay! Happy " + str(n) 

else:
	print "Oh no! Sad " + str(n)

happy_checker(123)

    
